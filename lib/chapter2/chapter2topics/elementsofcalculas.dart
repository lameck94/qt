import 'package:flutter/material.dart';

class ElementsOfCalculas extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Elements of Calculas'),
      ),
    );
  }
}
