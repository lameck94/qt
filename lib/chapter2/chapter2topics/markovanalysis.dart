import 'package:flutter/material.dart';

class MarkovAnalysis extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Markov Analysis'),
      ),
    );
  }
}
