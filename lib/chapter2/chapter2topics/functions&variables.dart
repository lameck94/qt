import 'package:flutter/material.dart';

class FunctionsAndVariables extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Functions & Variables'),
      ),
    );
  }
}
