import 'package:flutter/material.dart';

class InputOutputAnalysis extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Input - Output Analysis'),
      ),
    );
  }
}
